#pragma once

extern GColor fgColor;
extern GColor bgColor;
extern GColor outerRingColor;
extern GColor batteryRingColor;
extern GColor windowColor;
extern GColor extColor;
extern GColor timeBgColor;

void palette_init();
