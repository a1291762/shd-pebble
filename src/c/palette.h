#pragma once

extern GColor fgColor;
extern GColor bgColor;
extern GColor outerRingColor;
extern GColor batteryRingColor;
extern GColor windowColor;
extern GColor extColor;
extern GColor timeBgColor;
extern GColor innerBandColor;
extern GColor innerBandBrightColor;
extern GColor innerBandLineColor;
extern GColor innerBandLineBrightColor;
extern GColor blobColor;
extern GColor blobBrightColor;
extern GColor counterDotColor;
extern GColor expandingColor;

void palette_init();
